//変数の宣言・値の代入（体重）
let bodyWeight = 68;

//コンソールへの出力
console.log(bodyWeight);

//変数の宣言・値の代入（身長）
let bodyHeight = 1.7;

//コンソールへの出力
console.log(bodyHeight);

//変数の宣言・値の代入（計算式）
let num = bodyWeight / (bodyHeight * bodyHeight);

//コンソールへの出力
console.log(num);
